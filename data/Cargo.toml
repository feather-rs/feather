[package]
name = "feather-data"
version = "0.5.0"
authors = ["caelunshun <caelunshun@gmail.com>"]
edition = "2018"
publish = false

build = "build.rs"

[build-dependencies]
reqwest = { version = "0.10", features = ["blocking"] }